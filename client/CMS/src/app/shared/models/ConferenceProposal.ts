export class ConferenceProposal {
   proposalID: number;
   conferenceID: number;

  constructor(proposalID: number, conferenceID: number) {
    this.proposalID = proposalID;
    this.conferenceID = conferenceID;
  }
}
